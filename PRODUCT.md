# Product

## Register

brand

> 長篇內容型網站：十個章節的資料敘事，設計本身就是產品的一部分。
> 一章一頁，入口頁是目錄兼摘要。
> 第九章的 DuckDB-WASM 查詢器是工具，但它是十分之一，不是主體。

## Users

三種人，需求不同但可以用同一個版面服務：

- **技術讀者**（資料工程、統計、環境領域）——會讀方法段落、會點開表格、會質疑
  面板選擇。他們要的是能快速掃到「用了什麼樣本、什麼門檻、誤差多大」。
- **非技術讀者**（關心空氣品質的一般人）——只讀圖與粗體句。他們要的是不必懂
  統計也能拿到正確的印象，而且不會被過度簡化誤導。
- **審閱者**（可能是雇主、同行）——想在三十秒內判斷這份工作是否嚴謹。他們看的是
  數字有沒有出處、保留條件有沒有寫出來、以及介面本身是否經得起看。

閱讀情境：桌機為主，手機是次要但必須完整可用（375px 起）。
**不再是「長時間滾動」**——十章各自成頁，入口頁負責讓人在三十秒內看完全部論點。

## Product Purpose

把 1982–2025 年、3.41 億筆台灣空氣品質逐時觀測的重新分析，變成一份可以讀完、
每個數字都能自己重算的公開文件。它同時是一份 side project 作品集，不是學術產出：
不追求 DOI 與投稿格式，但嚴謹度是它唯一的賣點。

成功的樣子：一個懂統計的人讀完之後，找不到一處「這裡他偷懶了」。

## Brand Personality

**精確、克制、不迴避。**

- **精確**：每個數字都有樣本、期間與門檻跟著走。沒量過的不寫。
- **克制**：介面安靜，資料說話。顏色只用來編碼濃度，其餘一律灰階。
- **不迴避**：陰性結果、測不到的事件、自己犯過的錯，都用同樣的字級寫出來。
  這是這個專案唯一真正的差異化，版面必須讓它看得見，而不是藏在註腳。

語氣是研究報告，不是新聞報導。標題命名「被分析的量」，不是帶轉折的結論。

## Anti-references

- **新聞標題句法。** 短句、換行、轉折（「台灣的空氣變好了。但沒有一個地方達標。」）。
  擁有者明確要求避開；2026-07-29 已全站改寫過一次。
- **儀表板式的 KPI 磚牆。** 大數字 + 小標籤 + 漸層重複排列。這裡的數字需要
  單位、樣本與保留條件，脫離脈絡的大數字正是本專案批評的東西。
- **科普網站的親切化。** 表情符號、擬人化、「你知道嗎」式引導。讀者被當成成年人。
- **任何把不確定性藏起來的視覺手法。** 補起來的斷線、沒有樣本數的百分比、
  沒有基準線的 skill 分數。空值是發現，版面必須讓它看起來像發現。

## Design Principles

1. **顏色只編碼濃度。** 這是既有系統的核心約束，且它是對的：版面上唯一帶彩度的
   東西是資料。介面色（背景、線、文字）一律中性。任何新元件都遵守。
2026-07-30 起色票為 **Morandi**：濃度色階的色相順序不動（那是編碼本身），
   彩度落在 OKLCH 0.052–0.092。**中性色帶 C 0.004–0.010 的極淡色偏**（朝色階的
   冷端 h 250），不是 chroma 0——正中性的灰是唯一會被讀成「死掉」的灰，
   而莫蘭迪的灰從來不是中性的。這個量級比色階低一個數量級，在能指認色相的門檻之下，
   所以整頁唯一「有顏色」的東西仍然是資料。
   **預設主題為亮色**：這是一份在白天讀二十分鐘的長文，不是一個工具。
2. **保留條件與數字同級。** 樣本數、期間、門檻不是註腳字級。一個沒有分母的
   百分比在這個網站上是缺陷。
3. **密度是專業感，不是擁擠。** 讀者要能一眼掃過大量數字。密度靠對齊、
   tabular-nums 與穩定的間距節奏達成，不是靠縮小字級。
4. **斷裂必須看得見。** 圖上的每一個中斷都是真的中斷。沒有任何視覺手法可以
   讓缺漏看起來連續。
5. **底圖是脈絡，不是資料。** 測站地圖的縣市界用髮絲線與幾乎看不出來的填色，
   因為它不是量出來的東西——整張圖上唯一帶彩度的仍然只有那 73 個點。
   界線來自內政部國土測繪中心的官方圖資（1:5000，簡化至約 165 公尺），
   正確性靠一個會失敗的測試保證：**每一個測站都必須落在它自己的 metadata
   宣稱的那個縣市的多邊形內**。這是 73 項對外部來源的獨立斷言，跑在 CI 上。
   唯一的例外是縣市界本身：它是「區分縣市」的唯一手段，所以用專屬的
   `--map-line`（對底 3:1）而不是 `--line`（1.28:1），界線在這裡是資訊。
6. **可讀性優先於精緻。** 若「優雅的淺灰」與 WCAG AA 衝突，AA 贏。這份文件
   的價值在於被讀完。
7. **資料的色階不遵守莫蘭迪的邏輯，介面才遵守。** 莫蘭迪之所以安靜是因為
   *明度相近*；把這條規則套到資料編碼上，編碼就失效了——而它曾經被套上去：
   每個色相都解在「還能通過 4.5:1 的最亮明度」，那個條件每個色相只有一個解，
   於是七個色全部落在明度 0.018 的範圍內。**七個等明度的色相說不出「由低到高」**
   （色相讀成類別、明度才讀成數量），而且相鄰等明度色相之間沒有明度邊緣可對焦，
   看久了會累——這正是「配色看得不舒服」的來源，不是彩度太高。
   現在：介面是莫蘭迪（低彩度、帶色偏的灰、明度相近），**資料色階不是**——
   跨整條色階明度單調變化 0.21，順序同時由色相與明度承載。
   淺色主題由淺到深、深色主題由深到淺，因為色階必須離開它的紙。
8. **一個顏色當「記號」與當「文字」是兩個需求，所以有兩組 token。** 色階的淺端
   是刻意淺的（那是明度順序的來源），而淺正是文字會失敗的地方。`--cN` 給線與點
   （非文字 3:1），`--cN-ink` 由 `--cN` 混向該主題的反端推導而來給文字（4.5:1）。
   推導而非另選，兩者才不會走鐘。
9. **十章各自成頁。** 一次滾完量到 38,291px（42 個螢幕），那不是長篇、
   那是沒有結構。章節順序、編號與導覽只有一個來源（`lib/chapters.ts`），
   因為手動同步已經出錯過一次：插入一章時要改八個檔案的章號。
10. **一欄到底：欄寬就是行寬。** 同一個問題試了三次才對——
   (1) 正文受限、靠左放在 1,145px 的欄裡 → 右邊固定空 **326px**，讀成「沒填滿」；
   (2) 把受限區塊置中 → 餘裕對半了，但買到一個**縮排**：量到正文在自己的標題與圖
   右邊 **132px**，那讀起來像作文的首行，不像邊界；
   (3) **容器本身就是行寬**（`max-width` 由 `--measure` 加它自己的 padding 推導而來，
   兩者不可能走鐘）→ 標題、正文、圖表共用**同一個左緣、同一個寬度**，
   餘裕落在欄外，那才是頁面邊界該在的地方。欄內沒有任何東西比欄窄，所以不會有單邊空白；
   也沒有任何東西縮排。圖表為此讓出寬度（882px 而非 1,145px），這個交換是對的：
   圖是流體的、375px 就可讀，而**「對齊隨元素類型改變」是每個讀者都看得到的事**。
11. **章節用側邊欄，不用下拉選單。** 同一個需求走了三步，前兩步都量過為什麼不行：
   (1) 十個章節名擠成一列 —— 1,144px 的空間放 1,017px 的標籤，每個連結是 25px 高的
   純文字、沒有框（低於 44px 觸控下限），而放大塞不進去（正文字級要 1,128px、
   加章號要 1,302px）。那一列不是太小，是**超載**；
   (2) 收進 `<details>` 選單 —— 尺寸對了（每列 54–61px），但只服務「想到要去翻」的人，
   而**沒有人會想到**；
   (3) **側邊欄** —— 解法是它根本不是一個控制項：十章就在頁面上、當前那章有標記，
   沒有東西需要被發現，也沒有東西需要被打開。
   當前章標記三重（墨色、字重、前緣一條 inset shadow——不用 `border-left`，
   那會把圓角列切掉一邊）。手機以下用**一個 checkbox** 當抽屜：不寫任何腳本，
   所以 JS 壞掉時側邊欄仍可用（本檔的硬要求）。checkbox **不加**
   `aria-hidden`／`tabindex="-1"`——`<label>` 不可聚焦，那樣會讓抽屜沒有鍵盤路徑。
   閱讀進度改成側邊欄邊緣的一條垂直線；抽屜模式下隱藏，因為那時它不在畫面上。
   側邊欄與抽屜開關**互斥**：768–1920px 每個測到的寬度都只有一個成立。
12. **唯一的入場動畫只動位置，不動透明度。** 第一版用 `opacity: 0` 淡入配
   `animation-fill-mode: backwards`，量到的結果是**空白的首屏**：瀏覽器面板未顯示時
   `document.visibilityState` 是 hidden、`document.timeline.currentTime` 停在 0，
   動畫 `playState` 說 running 但時鐘不走，backwards 就把 `from` 的透明度 0 一直held 住。
   任何沒有可見視窗的算繪（背景分頁、無頭截圖、爬蟲、列印）都會拍到空的。
   只動 `translateY` 是把失效模式**移除**而不是防守它：時鐘凍在 0 時內容完全可讀，
   只是少位移七個像素。這條規則優先於任何動效想法。
13. **版面用容器查詢，不用視窗查詢。** 側邊欄吃掉固定一份寬度、閱讀欄又鎖在行寬上，
   所以**視窗寬度已經不再代表可用空間**。首屏那個兩欄圖文原本寫 `@media (min-width: 52rem)`，
   那是欄寬還有 1,145px 時寫的；量到的後果是：1,440px 視窗下右欄只解析到 357px、
   長到 863px 高，而地圖只有 655px，地圖下方留下 **271px 的空白**；
   1,100px 下右欄 201px、1,604px 高、空白 **1,012px**。斷點在讀一個已經不描述那個盒子的數字。
   改成 `container-type: inline-size` ＋ `@container` 之後，四個桌機寬度的欄高差是 **0**。
   另外：欄寬給地圖的部分是按地圖需要給的，不是按比例分的——地圖是高度預算制、
   且**不得低於 260px 寬**（低於此縣市名的碰撞測試就失去保證）。
14. **不為了密度犧牲行距。** 「減少無效空白」的正解是拿掉**結構性**的浪費
   （欄高不匹配、重複七次的 126px 分隔、首屏頂部 46px 的帶狀空白），
   不是壓縮段距。實測全站的節奏是段落間 21px、區塊間 34px——在 21px 的字級下
   那已經是 1.0/1.6 倍行距，再壓就是拿可讀性換密度，而本檔第 3、6 條說的正好相反。
15. **中文正文齊行；章節轉換只用一條線。** 中文每個字寬一個 em，斷行必然落在字框的
   整數倍上，所以**齊行不需要拉伸任何東西**（英文齊行只能拉伸詞間空格，這是它常靠左
   的理由）。`text-align: justify` 加 `line-break: strict`（完整禁則，句號與下引號
   不出現在行首）是正文的預設，範圍限正文——表格、圖例、控制項有自己的排列規則。
   段落分隔一律用 `<p>`，**不用 `<br><br>`**：`<br>` 造成的短行是強制斷行，
   齊行對最後一行無效，量到的最糟六個區塊全是這個原因。
   章節轉換只有**一條線**，而且是章節標記自己那條，所以線與替它命名的數字是同一個
   物件；`padding-block` 刻意不對稱，因為線**上方**的空白才是分隔章節的。
   每個邊界的間距必須相同（量到 spread 0），這需要清掉上一章結尾元素帶來的尾距。
16. **對比要用 APCA 量，WCAG 只是法定下限。** 「字太淡」這個抱怨在 WCAG 下量不出來：
   `--text-muted` 有 6.67:1、`--text-faint` 有 5.00:1，全部合格，讀起來還是累。
   WCAG 2.x 的比值來自一條從未針對閱讀舒適度校準的亮度公式；APCA 的 Lc 是，
   而同樣這兩個 token 在 Lc 上是 **74 和 66**（90 = 流暢正文、75 = 正文下限、
   60 = 僅適用大字）。**深色模式是兩者中較差的那個，而 WCAG 說的正好相反**：
   WCAG 給深色 muted 8.95:1（比淺色的 6.67 高），APCA 給它 **Lc 60.8**
   （比淺色的 74.4 低）。比值公式系統性地高估深色模式，所以量到「AAA」的那個主題
   才是讓讀者累的那個。三個 ink 重解到 Lc 92 / 80 / 70，且是解在**三個表面中最差的那個**上。
   連結是最後一個沒被發現的：`--accent` 在深色是 Lc 47.4，低於「文字要能承載意義」
   的 60 底線——而連結正是讀者換章節的方式。
17. **飽和度和可分辨度是兩個問題，混在一起會白做一輪。** 「圖表顏色辨識度太低」
   聽起來像是「顏色太灰、加飽和度」，但實測：固定色相與明度、把七階全部推到
   各自色域上限的 70/80/88%，最差相鄰步距只從 **3.5 動到 4.0**。
   飽和度決定一個記號好不好**命名**（對得上圖例），不決定兩個鄰居好不好**分開**，
   而 ΔE 只量後者。真正的槓桿是**色相間距**：舊色階的註解聲稱它「從不依賴紅綠對比」，
   但 h84 → h56 → h30 就是紅綠軸，加上鄰居只差 0.035 明度，
   模擬（Machado 2009）量到 c3/c4 在綠色盲下 **ΔE00 3.5**——同一個顏色，
   而那正是台灣 24 小時標準兩側的兩個級距。解法是明度階梯改成等距（0.0465）、
   色相在錨點 ±20° 內求解、色度單調上升，目標是讓**一般、綠色盲、紅色盲三種人同時**
   看到的最小相鄰步距最大化。結果：綠色盲 3.5 → 8.2、紅色盲 5.4 → 10.3。
   兩個誠實的上限要寫下來：**一般視覺沒有改善**（11.5 → 11.1，七個有序級距在這個
   明度帶裡就是 11 上限），所以幫到一般讀者的是色度（識別）和**不要讓任何一張圖去分辨七階**；
   藍黃色盲略微變差（5.0 → 4.4），因為這條弧線本身就是藍黃軸，失去它之後只剩明度階梯——
   而明度階梯現在是由構造保證的。綠+紅色盲合計約男性 8%，藍黃約 0.0001%。
18. **序列色階不可以當類別色用。** `ChapterHealth` 的四條線原本取 c5/c4/c3/c2——
   序列色階的四個**相鄰**成員拿去當四個類別，而相鄰成員之所以相鄰就是因為它們
   本來就該看起來像。量到最近的一對是一般視覺 ΔE00 11.1、綠色盲 8.2，
   而那張圖的全部論點就是「你看得出四條線分開」。改成每隔一階取（c6/c4/c2/c0），
   明度差加倍、順序不變、最近一對變成 25.7 / 9.1。
19. **參考線不帶色相。** WHO 指引線與台灣標準線原本是 h210 與 h60，
   量到 `--who` 距 `--c1` 只有 ΔE00 7.4：一條輔助線穿過同一張圖，
   身上穿著幾乎就是某個資料系列的顏色。改成兩個中性明度（各自過 3:1），
   於是**色相只代表濃度，不代表別的**，而兩條線本來就各自帶著自己的文字標籤。
20. **語域：標籤要精確，正文要書面，兩者是不同的毛病。** 業主指出「自己查」與
   「最髒的日子」太口語，這兩個例子剛好各示範一種：
   **標籤命名的是一個被量出來的東西，所以必須把它命名準確**——
   「當年最糟的一天」是 `max(日均值)`，所以叫「當年日均最高的一天」；
   「十大最髒的日子」是 `ORDER BY mean DESC LIMIT 10` 的站×日，所以叫
   「PM2.5 日均值最高的十個站日」；「1 = 最乾淨」是 `rank(annual_mean)` 升冪，
   所以叫「1 = 年均最低」。評價詞代替測量值，就是把讀者能核對的東西換成不能核對的。
   **正文則是語體問題**：「掉到」改「降到」（同檔第 65 行本來就寫「降到」）、
   「亂劃」改「任意劃定」、「踢掉……了事」改「排除……即可」、「虛胖」改「高估」。
   反過來，下面這些**刻意不動**，動了就是把清楚的句子換成官樣文章：
   - `V掉` 結果補語（抽掉、丟掉、換掉、抹掉、刪掉、挖掉、解釋不掉）是標準書面中文，
     與「掉到」不同——後者是「下降」的動詞，而同一個檔案本來就有書面說法。
   - 反身的「自己」（「該站自己的安慰劑均值」、「測站自己的歷史」、「原文自己刊的
     數字」）是所有格，不是「自己查」那個「自己＋動詞」的對話句式。
   - 「跑」模型／程序是統計圈固有術語，而本文件還**引用**「分區各跑一次」作為 2018 年
     的做法名稱；把周邊改成「執行」而引號內留著「跑」會讀起來像錯字。
     只有 ChapterData 的「跑一次 twair ingest」改成「執行」，因為那是 shell 指令。
   - 被引述來檢查的口語說法（「那是『不知道』，不是『乾淨』」）是聲音，不是瑕疵。
   - 下一句就給出答案的修辭問句（「被丟掉的是什麼？」）是說明文的正常寫法。
21. **要求「不要列出 X」時，先問 X 是不是那一章存在的理由。** 業主說資料查詢頁
   「不需要列出 SQL 語法」。那頁攤開的 `<textarea>` 有 **362px**，在讀者還沒要求
   任何東西之前就佔住版面中央——對多數讀者，選單裡的那個問句才是內容，表格才是答案。
   但可編輯的查詢正是這一章不只是六張固定圖表的原因，所以是**收進 `<details>`
   預設關閉**，而不是刪掉：關閉時整塊 41px，展開時 +389px，查詢一字未動。
   用 `<details>` 而不是 JavaScript：這頁的前提是按下按鈕之前什麼都不下載。
22. **元件不會自動繼承對它有意義的樣式。** 上面那個 `<details>` 一寫出來就套上了全站
   `details` 的「門」樣式——邊框、抬升面、內距、自己的箭頭——那是為第八章六張證據表
   設計的（審閱者必須看得出證據在那裡）。結果這個本該像註腳的面板被畫成一顆有框的按鈕，
   正是兩輪前才從導覽拿掉的那個控件。**只拿掉盒子，不重新定義箭頭**：
   第一版重寫了 `::before`，於是第八章的箭頭關閉時朝右、這裡的關閉時朝下——
   同一個站台兩種相反的慣例。量到 `chevronsAgree: true` 才算修好。
23. **一欄的表頭和它的值必須對在同一邊。** 「歪歪斜斜」不是模糊的感覺，是三個可以量的
   缺陷疊起來：`renderTable` 只把 `num` 類別加在 `<td>` 沒加在 `<th>`（CSS 早就有
   `th.num` 規則在等），所以每一欄的標籤在左緣、數字在右緣，量到 **106 / 272 / 125px**
   的分離；NULL 的破折號沒有類別，在右對齊欄裡靠左，離同欄數字 **398px**；
   小數點沒有對齊，同一欄裡 `0.6` 的點比 `0.637` 的點右移 **32px**。
   對齊要**由欄位的宣告型別決定，不能靠猜值**：`normalise` 已經把 DATE 變成字串、
   BIGINT 變成數字，所以到了表格裡，年份、次數、測量值全都只是 `number`，
   而日期和測站名無法分辨。只有 Arrow 宣告的型別還知道誰是誰。
24. **小數點要對齊，但不可以補零。** 把 `0.6` 寫成 `0.600` 是無中生有兩位精度，
   而 `duck.ts` 特意把 Float32 截到七位有效位數就是為了避免這件事。正解是用
   **和它所替代的東西一樣寬的空白**，而且要實測而不是假設：
   在算繪出來的字體裡 U+2007 FIGURE SPACE 量到 **10.875px＝一個數字**，
   U+2008 PUNCTUATION SPACE 量到 **4.375px＝一個小數點**（普通空白與 NBSP 是
   5.531px，不能用）。所以一個沒有小數的值可以用「一個點寬 ＋ n 個數字寬」的空白
   站住位置，整欄的小數點連成一線，卻沒有多寫任何一位數字。
   負號用 U+2212（散文各章本來就用它），在等寬數字下它是一個數字寬，
   ASCII 連字號只有 8.06px。
25. **千分位是按欄決定的，因為年份就住在四位數。** 沒有任何規則能從型別分辨
   `year` 和 `count(*)`——兩者都是整數。所以規則是：**整欄只要有任何一個值達到五位
   整數就整欄加千分位，否則整欄都不加**。1997 保持 1997，341442552 變 341,442,552，
   而同一欄不會同時出現 1213 和 34,125。
26. **報表的框要貼著表，不是表撐滿框。** 全站的表是 `width: 100%`，因為它們待在為
   它們量好的圖框裡；查詢結果不是，它的形狀是讀者問出來的。`width: 100%` 加
   `white-space: nowrap` 時，瀏覽器按各欄最寬內容的比例分配剩餘寬度——於是
   **欄名最長的那一欄拿到最多空白**，它的值離自己的標籤最遠。量到 186 / 484 / 211px，
   而那 484px 是欄名造成的，不是資料造成的。`fit-content` 讓每欄縮到內容，框就停在那裡。
   表格的行距也刻意比正文緊（1.7 → 1.45，列高 60 → 48px）：這不是本檔第 14 條拒絕的
   那個取捨，第 14 條講的是正文，而表格是用來**往下掃**的，不是用來讀的。
27. **斑馬紋要離開紙面，不是浮在紙面上。** 舊的紋是把 `--bg-raised` 混向透明，
   等於在近白色上再抬一層近白色，實測合成後只有 **1.031:1（淺）／1.064:1（深）**——
   那個幫助視線追行的裝置其實什麼都沒做，而這是寬表讀起來像一堆散落數字的原因之一。
   紋必須**遠離紙面**，所以方向隨主題翻轉，而且兩者剛好都落在既有的表面 token 上：
   淺色是下沉、深色是抬升。值不是憑品味挑的，是**由坐在它上面的最淡墨色決定**：
   紋是第四個表面，而 `--text-faint` 當初只解過三個，所以取「`--text-faint` 仍維持
   APCA Lc 70 的最遠點」。再深一點，就是讓紋去吃掉躺在它上面的 NULL 破折號。
28. **截斷的答案一定要說自己被截斷了。** 500 列的上限原本是靜默的：一個命中四萬筆的
   查詢畫出 500 列，狀態列寫「500 列」。這在這個站台是最不能犯的一種錯——
   整頁存在的理由就是讓讀者自己核對數字，而一個不說自己被截斷的答案，
   等於邀請他們拿錯的分母去核對。`runQuery` 現在回報真實列數與是否截斷。
   零列也是一個答案，不是壞掉的表：原本會畫成一條有邊框的欄名帶子壓在空無之上。
29. **DECIMAL 會印出錯的數字，而且看起來很合理。** Arrow 把 DECIMAL 交回來時是一個
   `DecimalBigNum` 物件，它的 `String()` 是**未縮放的整數**——所以 `SELECT 0.6`
   到表格裡是 6，而在 `VALUES` 清單裡（DuckDB 會把整欄擴寬成同一型別）
   (0.6, 0.637, 0.5834) 變成 DECIMAL(5,4)，印出 6000、6370、5834。
   它是物件，所以連 `bigint` 那條分支都沒接住它。縮放要用**字串手術**而不是除法，
   因為 DECIMAL(38,6) 過不了 double。
30. **一列的欄位要按位置讀，不能按名字讀。** 原本是 `toArray()` 之後 `record[name]`，
   於是兩欄同名時靜默給出錯的資料：`SELECT 1 AS a, 2 AS a` 印出兩個 1，
   而 `SELECT a.date, b.date FROM x a JOIN y b`——這個 schema 上再普通不過的 join——
   兩欄印出同一個日期，`a.mean, b.mean` 則會讓 PM10 欄顯示 PM2.5 的數值。
   名字在這裡不是鍵，位置才是。
31. **長而窄的表要排成欄，那是印刷早就給過的答案。** 三欄 × 29 列的結果實測寬 375px，
   坐在 882px 的閱讀欄裡：**用掉 44% 的寬度、490px 空著，而 29 列裡有 19 列被藏在
   546px 盒子裡的捲軸後面**——版面同時浪費了寬度又不夠高度。而那張表**並排放得下兩份**。
   把列流進並排的欄位，就是把浪費掉的寬度換成短缺的高度：實測 44% → **85%**、
   捲軸消失、29 列全部看得見。
   守則是「只對這一種形狀成立」，所以兩個門檻都要有：
   欄數超過六就不切（寬表沒有好處，只會開始橫捲）；而**每段有列數下限而不只是段數上限**——
   把 15 列切成三欄各 5 列是花招不是版面。29 列在 4 段可容的寬度下仍然只切 3 段，
   因為每段 8 列的下限先綁住了它，那是刻意的。
   三件必須跨段一致的事：**欄寬**（量一次未切分的整表，所以已經容得下整欄最寬的值，
   任何一段都不會溢出）、**小數補白與千分位**（必須用整份結果算，
   否則同一欄會在左半印 0.6、右半印 0.60，兩半就不再對齊了）、
   以及**外框只有一個**（每段各自加框會讀成兩三張無關的表；框屬於整組，段之間只有一條線）。
   段數的算式必須和 CSS 的間距一致：算式編了 26px 而網格是 0，
   二欄結果就會在四段放得下時只切三段、用掉 61% 的寬。
32. **一列只用一種分隔。** 每列同時有一條 hairline 和一條斑馬紋，
   等於把粗體字再加下劃線。留斑馬紋：它能沿著整列穿過並排的段落，
   而不會在段與段之間那條縱線上再畫一條橫線。表頭那條界線相反，要留實線並加重
   （`--rule`）——它分開的是「欄名」與「資料」，而且捲動時必須留在原地。
33. **版面依賴量測時，量測要能重做。** 排法取決於有多少空間，而空間會隨視窗改變。
   重跑查詢來重排是荒謬的——列就在手上。所以結果本身要留著，`resize` 時只重排；
   而防止拖動視窗時重建五百列表格的守衛不是計時器，是**決策本身**：
   先算現在該排成幾段，只有和螢幕上的不同才重畫。
   實測往返：1440 兩段 → 375 一段（332px 進 333px，無捲動）→ 1440 兩段，
   29 列不增不減、順序不變。
34. **字級要看樓地板，不是看內文。** 內文 21px 已經不小，而業主仍說「整個網站的字都
   還是偏小」。量了才知道為什麼：**624 個文字節點裡有 462 個（74%）比內文小**，
   而全站最常見的字級根本不是內文的 21px，是 `--text-sm` 的 20.2px——**單一級距就佔 375 個節點**。
   讀者實際體驗到的是樓地板，不是內文。所以整條尺規一起 +12%
   （root 21 → 23.5px、圖表註記 19 → 21px、最小的 18 → 20px），
   比例維持不變，於是階層不動而地板抬高：最小字級 18px → 20px（1440）、16.5 → 18.7px（375）。
   放大字級會撞到版面，所以三件事必須量過才算數：
   **中文每行字數**（39–42，仍在 35–45 的舒適帶）、**標題溢出**（0）、
   以及**地圖縣市名的碰撞**（15 個標籤放大 11% 之後，重疊對數仍是 0）。
35. **側邊欄的寬度是由最長的標題決定的，不是由品味決定的。** 字放大之後，
   1366×768 下側邊欄從 768px 的視窗長到 **934px**，於是 `/data` 的「當前章節」標記
   ——側邊欄存在的唯一理由——掉到摺線下 **114px**。原因量得出來：
   六個九字標題各需要 203px 的標籤寬度，卻只拿到 170px，於是每一個都折成兩行；
   **不折行的話側邊欄只有 607px**。所以下限從 13rem 改成 **13.9rem**，
   那是最長標籤加上編號欄、間距與兩側內距**解出來**的寬度，不是估的。
   結果：1280／1366／1440 三個寬度下折行 0/10、溢出 0px、標記都不必捲動就看得到。
36. **紙要是有顏色的紙，不是沒上色的灰。** 業主連續三輪說配色不好看，而量出來的問題
   不在色階而在**紙**：`--bg` 距離同明度的純灰只有 **dE00 1.44**，而一個大面積要被看成
   「有色調」大約需要 2 以上。也就是說整個版面在讀者眼裡就是**未上色的灰**，
   我自己的註解甚至寫過「純灰是唯一會被讀成死掉的灰」，然後把 chroma 設成 0.004。
   界面色度提高到讓每個大面積都落在 **dE00 ≈ 3**（實測 channel spread 5 → 10），
   仍比色階低一個數量級、仍不會有人叫得出色名，但紙終於是紙。
   **色相選 250**（色階的「乾淨」那一端），所以連紙的顏色都還是那套編碼的一部分。
37. **想讓色階更好看之前，先量它會不會變難分辨。** 色階走了 **274° 的色相**，那是彩虹，
   而彩虹色階向來被批評為既不感知均勻也不好看。所以我試著把弧線縮短、把色度改成
   平滑上升——**實測結果是色盲最差步距從 8.2 掉到 6.3**。為了好看而讓 8% 的男性看不出
   相鄰級距，這個交易不能做，所以色階的色相與色度**維持不動**。
   唯一免費的一步是最末端：把 c6 從洋紅 338° 移到 **346°**，淺色主題下
   色盲步距 8.2 → 8.2、一般 11.1 → 11.1、3:1 餘裕不變，弧線 274° → 266°，
   而且落在台灣 AQI 真正在用的褐紅而不是洋紅。免費的才拿。

38. **一個從來沒被設定的 token，會安靜地把每一張圖都畫小。** `--y-w` 在整個原始碼裡
   只出現一次——而且是 `var()` 的**使用**、帶著 fallback，從來沒有人**定義**它。
   於是每一張圖的 y 軸留白都固定是 `3.4 × --chart-tick`＝71px，不管標籤寫的是「50」
   還是「100%」。實測 375px：繪圖區只有 **140px，而卡片是 244px（57%）**。
   `labelGutter()` 這個 helper 早就存在、也早就被 `--key-w` 用著，只有 y 軸沒接上。
   接上之後留白 71 → **36px**，繪圖區 140 → **175px（72%）**，1440 下 88% → 92%。
   帶 fallback 的 `var()` 是最難發現的一種缺陷：它永遠有值，所以永遠不報錯。

39. **容器查詢不能查自己。** `container-type` 放在 `.hero-grid` 上，而 `@container`
   區塊裡又寫 `.hero-grid`——元素不能查詢它自己建立的容器。危險的是**它不是全部失效**：
   同一個區塊裡也有 `.hero-grid > .map-wrap` 這種規則，而**子元素確實在容器裡**，
   所以子元素的 `grid-area` 生效了、而該替那些欄位定尺寸的 `grid-template-columns`
   沒有。結果 1440 下是兩欄 auto 尺寸的 **446px／499px**（而不是設計的 clamp），
   要求橫跨整列的 `.map-notes` 卡在 **446px**。把 `container-type` 移到一層 wrapper：
   欄位變成設計值 **376px／555px**、`.map-notes` **983px 真的橫跨**、首屏矮了 253px。

40. **細化軸標籤的門檻要隨標籤數量走。** 一條 `36rem` 的門檻是為九個年份標籤挑的，
   但 36rem 是 846px，而全站最寬的 `.plot-x` 只有 715px——所以那一階
   **無條件恆常開啟**。第八章一條 602px 寬、五個標籤的軸永遠只顯示
   `1h / 4-12h / >48h`，把 `2-3h` 與 `13-48h` 藏起來，而那兩個名字全頁再無他處出現。
   改成用 `data-n` 帶入標籤數，門檻＝數量 × 3.4rem，也就是「它們到底會不會疊到」
   的真正意思。現在 1440 下五個全顯示，375px 下才藏三個。

41. **十一頁裡有十頁沒有 `<h1>`。** 大綱從 `h2` 開始，是 axe 的 `page-has-heading-one`
   失敗，螢幕閱讀器使用者要求最上層標題會得到空的。每一章的標題升成 `h1`、其下各降一級
   （共 52 個標題），而字級尺規同步下移一階，所以**看起來不變、大綱才變成真的**。
   `#hero h1` 保留 display 尺寸，因為整份文件的標題和一章的標題不是同一個工作。
   最後一個跳級在 `/stations`：測站卡片的標題在 `StationPicker` 裡、不是 `Chapter*`
   元件，所以升級掃不到它。現在十一頁都是「剛好一個 h1、零個跳級」。

42. **躲起來的東西要連焦點一起躲。** 手機抽屜用 `transform` 移出畫面，但十一個連結
   仍然可以被 Tab 到——鍵盤或切換掃描的使用者從頁首開始按，會連續十一次看不到焦點環，
   畫面上沒有任何東西解釋。加 `visibility: hidden`，並讓它的 transition 延後 260ms，
   這樣滑出去的整段動畫期間抽屜還看得見，滑進來則立刻可見。實測關閉時可聚焦數 11 → **0**。
   反過來，skip link 用的是 `.visually-hidden`——**它沒有 `:focus` 的出口**，
   所以全站十一頁的第一個 Tab 停在 (-1,-1) 的 1×1 裁切盒裡。那個連結一直是對的、
   命名也對，只是從來沒有人看得見它（WCAG 2.4.7）。要解除的是**五個宣告全部**，
   不是只有 `clip`。

43. **在 JS 裡建立的元素拿不到元件的 scoped 樣式。** Astro 把 `data-astro-cid-*`
   蓋在**樣板裡的**元素上；錯誤訊息那個 `<pre>` 是 `document.createElement` 出來的，
   所以它從來沒帶過那個屬性，而全站唯一的失敗介面的唯一一條樣式從來沒有匹配過它。
   查詢失敗會渲染成瀏覽器預設的 `<pre>`：沒有框、沒有面、字體不對，375px 下
   把頁面推出去 297px。改成 `.result :global(.error)`。同一支流程還有兩個：
   失敗時 `setStatus("")` 等於在 live region 上什麼都不播報，以及 `getDb` 會把
   **被拒絕的** promise 一起快取——連線中途斷掉的讀者再按一次，拿到一模一樣的錯誤，
   而且一個位元組都沒有重新請求。

44. **沒量過的效能宣稱，也是沒量過的宣稱。** `duck.ts` 寫著 range request 讓一次查詢
   「只抓幾 KB 而不是整個檔案」。可以量的那一半我量了：Pages 確實送
   `Accept-Ranges: bytes`、`.parquet` **沒有** gzip，中段 `Range` 回 **206 而且剛好 32 bytes**。
   但另一半量不到——DuckDB 是在 Web Worker 裡發請求的，主執行緒補丁過的 `fetch`
   和頁面的 network log 都看不見。所以那句話是對某個函式庫的合理相信，不是誰查證過的事，
   註解改成只說量到的部分。

## Accessibility & Inclusion

- **兩個門檻，不是一個。WCAG 2.1 AA 是硬性下限，APCA 是實際目標。** 正文對比
  ≥ 4.5:1、大字 ≥ 3:1 仍然必須成立，但**通過它不代表讀起來舒服**——見設計原則第 16 條。
  現在全站的目標是 APCA Lc：正文 92、次要 80、註記 70，且解在三個表面中最差的那個上。
  十一頁 × 深淺兩色各量 1,433 個文字節點，**Lc 低於 60 者 0 個**
  （改之前深色有 26 個，最低 47.3）；最差 Lc 淺色 69.8 / 深色 70.1，
  最差 WCAG 淺色 5.24 / 深色 9.13。
- **色盲要模擬，不能只靠「沿藍—黃軸排序」這句話。** 舊色階的註解就是這麼寫的，
  而它是錯的（見第 17 條）。任何新的色階或類別色必須用 Machado (2009) 模擬
  綠色盲、紅色盲、藍黃色盲並量 CIEDE2000，最差相鄰步距至少 8。
  現值：綠色盲 8.2（淺）/ 10.7（深）、紅色盲 10.3 / 9.5。
- **`prefers-reduced-motion`**：已全域處理，新增動效必須提供替代。
- **無 JavaScript 可讀**：圖表在建置期產生 SVG，不依賴前端繪圖套件。
- **任何寬度都不需要橫向捲動來看圖**：圖表為流體版面（標記走 SVG、文字與資料點
  走 HTML 疊層），在 375px 到 1920px 之間都完整可見，且標籤恆為 20–21px。
  只有多欄資料表仍會在自己的容器內捲動——那是不得不然，不是預設。
- **375px 無橫向溢出**：頁面本身永不橫捲。十一頁全部量過，深淺兩色皆為 0。
- **最小字級 18.7px（375px）／20px（1440px）**：圖表註記曾經是 17px，
  比敘述它的句子小 19%，全站有 148 個文字節點落在那個尺寸。
  一張圖的註記不該是整份文件裡最小的字。
- **分界線也有對比下限，而且要解在最差的表面上**：章節分界是唯一的轉換標記，
  所以用專屬的 `--rule` 而不是 `--line`。它曾經解錯——0.635 是對著 `--bg` 解的，
  但這個記號也會坐在 `--bg-sunken` 上，量到 **2.92**。重解後淺深皆 3.01。
  `.zero`（「無效果」那條線）同理從 `--line`（1.31）改成 `--rule`（3.00）：
  整張圖的論點就是估計值離不離開它，那是資訊而不是格線。
- **色階本身也有兩個下限，而且是不同的兩個**：記號（線、點、色塊）走非文字的 3:1——
  量到最低 3.00（淺）／3.73（深）；文字走 `--cN-ink`，目標 APCA Lc 75，
  量到最低 75.0（兩色）。每個 `-ink` 的混色比例是**還能維持 Lc 75 的最大值**，
  這樣每個標籤保留自己系列的色相，而不是全部被壓到最弱成員需要的深度。
  深色主題必須自己重宣告這七個比例：它們是往 `--ink-mix` 的百分比，
  而 `--ink-mix` 在兩個主題是相反的極端，同一個數字意思相反。
- **有幾種記號是刻意低於 3:1 的，要點名而不是放寬門檻**：安慰劑分布
  （`.placebo`，數千條重疊細線，密度才是訊號）、無資料的斜線填充
  （`.hatch-line`，是紋理在說「沒有資料」）、地圖的底色縣市面
  （`.county` 的 fill 是表面，帶意義的是它的邊界 `--map-line`，量到 3.36）、
  以及極座標格之間的縫（`.cell` 的 stroke 就是 `--bg-sunken`）。
  探針要按名字排除這些，讓排除清單可以被審查。
- **量對比的工具本身要會做 alpha 合成，而且不能用正規表達式讀顏色**：
  Chrome 的 computed style 會原樣保留 `oklch()`，拿 regex 去讀會把
  `0.62 0.052 249` 當成三個 RGB 位元組——這曾經產生 1,077 個假的不合格。
  正解是把任何 CSS 顏色畫進一個 1×1 canvas 再讀像素。半透明表面必須先合成；
  色塊的探針要從**父層**開始往上走，否則它會找到自己而回報 1.00。
