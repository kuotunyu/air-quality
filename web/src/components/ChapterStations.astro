---
/**
 * Chapter 2 — one station at a time.
 *
 * Lifted out of `pages/index.astro` unchanged when the document became ten
 * routes.
 */
import StationPicker from "./StationPicker.astro";
import { cards, cigaretteCaveat } from "../lib/data";
---

<!-- ═══════════ 2. 你住的地方 ═══════════ -->
<section id="yours" class="page">
  <p class="eyebrow">第二章</p>
  <h2>測站個別統計</h2>
  <p class="lede">
    選一個測站，看它最近一個「資料完整到可以比較」的年份。
    已停用的測站給的是它停用前最後一個完整年份。
  </p>

  <StationPicker cards={cards} caveat={cigaretteCaveat} />
</section>
